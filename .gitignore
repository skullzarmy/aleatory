node_modules/
dist/
.env
.env.*
contract/output*/
contract/__pycache__/
# SmartPy writes one directory per test scenario into the working directory,
# named after the scenario. `npm run test:contracts` sets SMARTPY_OUTPUT_DIR so
# they all land in contract/output; running a suite by hand without it scatters
# them wherever you happened to be standing.
contract/output/
.DS_Store

# deploy scripts
.env
contract/build/

# next
.next/
next-env.d.ts
out/

# Local Netlify folder
.netlify
